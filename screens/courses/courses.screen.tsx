import { ScrollView, Text } from "react-native"

const CoursesScreen = () => {
    return (
        <ScrollView>
            <Text>
                Courses Screen
            </Text>
        </ScrollView>
    )
}

export default CoursesScreen;