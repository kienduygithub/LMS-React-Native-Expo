import { ScrollView, Text, View } from "react-native"

const HomeScreen = () => {
    return (
        <ScrollView style={{ flex: 1 }}>
            <Text>
                Home Screen
            </Text>
        </ScrollView>
    )
}

export default HomeScreen;