import { ScrollView, Text } from "react-native"

const ProfileScreen = () => {
    return (
        <ScrollView>
            <Text>
                Profile Screen
            </Text>
        </ScrollView>
    )
}

export default ProfileScreen;