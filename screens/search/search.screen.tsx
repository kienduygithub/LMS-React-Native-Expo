import { ScrollView, Text } from "react-native"

const SearchScreen = () => {
    return (
        <ScrollView>
            <Text>
                Search Screen
            </Text>
        </ScrollView>
    )
}

export default SearchScreen;